# Candidating

This web app helps you figure out what political candidates are right for you.

To set up, open a terminal and run `npm install`, then `npm start`.
Go to `localhost:3000` in your web browser to view the homepage.